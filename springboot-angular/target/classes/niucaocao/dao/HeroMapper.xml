<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="niucaocao.dao.HeroMapper">
	<insert id="save" parameterType="hero" useGeneratedKeys="true" keyProperty="id">

		insert into hero.hero
		(
		name
		
		)
		values
		(
		#{name}
		
		)
	</insert>
	<select id="findAll" resultType="hero">
		select id,
		     name
		from
		hero.hero;
	</select>

	<select id="findById" resultType="hero">
		select id,name
		from
		hero.hero
		where id=#{value}
		;
	</select>
	<update id="update" parameterType="hero">
		update hero.hero
		set
		name=#{name}
		where
		id=#{id};
	</update>

	<select id="findbyInput" parameterType="String"
		resultType="hero">
		select id,name
		from hero.hero
		<!--where 1=1 -->
		<where>
			<if test="heroName!=null and heroName!=''">
				and name like "%"#{heroName,jdbcType=VARCHAR}"%"
			</if>
		</where>
	</select>

	<delete id="delete" parameterType="int">

		delete
		from
		hero.hero
		where
		id=#{value};
	</delete>


</mapper>